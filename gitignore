# Jekyll's generated output — never commit this, GitHub builds it for you
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Ruby dependency folder if you ever run "bundle install --path vendor"
vendor/
.bundle/

# Mac system file
.DS_Store
